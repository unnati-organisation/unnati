---
// Decap CMS admin shell. The config lives at /unnati/admin/config.yml and is
// referenced explicitly so the editor works regardless of trailing slash.
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex" />
    <link href={`${import.meta.env.BASE_URL}admin/config.yml`} type="text/yaml" rel="cms-config-url" />
    <title>Content Manager</title>
    <link rel="icon" href="data:," />
  </head>
  <body>
    <script is:inline src="https://unpkg.com/decap-cms@^3.0.0/dist/decap-cms.js"></script>
  </body>
</html>